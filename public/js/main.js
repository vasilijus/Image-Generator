function onSubmit(e) {
    e.preventDefault();

    const prompt;
    const size;

    if(prompt === '') {
        alert('Please add some text');
    }
}

document.querySelector('').addEventListener('submit', onSubmit);